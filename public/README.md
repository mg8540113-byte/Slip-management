# Public Assets Directory

This directory is for static assets that will be served at the root of your domain.

Files in this directory will be accessible at `/filename.ext`

Example:
- `public/logo.png` → accessible at `/logo.png`
- `public/favicon.ico` → accessible at `/favicon.ico`
